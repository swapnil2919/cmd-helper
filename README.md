# cmd-helper
CMD Helper is a cross-platform CLI tool that lets you run simple English-style commands instead of remembering OS-specific terminal commands. It auto-detects your OS (Linux/Mac/Windows), handles interactive prompts when you skip arguments, and is fully configurable through config.json no coding needed to add new apps or commands.
